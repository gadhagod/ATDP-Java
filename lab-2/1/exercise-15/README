The class ContDemo in this chapter shows one way to use a for loop to print the even numbers from 0 to 100.  
// Use continue.
class ContDemo {
  public static void main(String[] args) {
    int i;

    // print even numbers between 0 and 100
    for(i = 0; i<=100; i++) {
      if((i%2) != 0) continue; // iterate
      System.out.println(i);
    }
  }
}